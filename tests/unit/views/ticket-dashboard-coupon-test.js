import { moduleFor, test } from 'ember-qunit';

moduleFor('view:ticket-dashboard-coupon', 'Unit | View | ticket dashboard coupon');

// Replace this with your real tests.
test('it exists', function(assert) {
  let view = this.subject();
  assert.ok(view);
});
